---
slug: mdx-blog-post
title: MDX 博客文章
---

博客文章支持 [Docusaurus Markdown 特性](https://docusaurus.io/docs/markdown-features)，比如 [MDX](https://mdxjs.com/)。

:::tip

利用 React 的能力，创作可交互的博客文章。

:::

{/* truncate */}

例如，使用 JSX 创建一个可交互的按钮：

```js
<button onClick={() => alert('按钮被点击了！')}>点我</button>
```

<button onClick={() => alert('按钮被点击了！')}>点我</button>
